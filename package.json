{
  "name": "m13-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --mode production --clearScreen false",
    "lint": "tsgo --watch",
    "build": "tsgo && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "update": "npx npm-check-updates -u && npm install"
  },
  "devDependencies": {
    "@typescript/native-preview": "^7.0.0-dev.20260426.1",
    "@vitest/browser-playwright": "^4.1.5",
    "@webgpu/types": "^0.1.69",
    "vite": "^8.0.10",
    "vite-plugin-solid": "^2.11.12",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4",
    "toad.js": "^0.3.2"
  }
}
